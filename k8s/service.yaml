apiVersion: v1
kind: Service
metadata:
  name: travel-apap-accommodation-fe-service
spec:
  selector:
    app: travel-apap-accommodation-fe
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80