apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: travel-apap-accommodation-fe-ingress
  annotations:
    kubernetes.io/ingress.class: traefik
spec:
  rules:
    - host: 2306211231-fe.hafizmuh.site
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: travel-apap-accommodation-fe-service
                port:
                  number: 80